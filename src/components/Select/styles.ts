import styled from 'styled-components';

export const Container = styled.select`
  width: 100%;
  max-width: 215px;

  padding: 17px 33px;

  font-weight: 500;
  font-size: 15px;
  line-height: 18px;

  border-radius: 05px;

  background-color: var(--white);

  margin-top: 30px;
`;
